planning_plugin: ompl_interface/OMPLPlanner
start_state_max_bounds_error: 0.1
jiggle_fraction: 0.05

request_adapters: >-
  default_planner_request_adapters/AddTimeOptimalParameterization
  default_planner_request_adapters/ResolveConstraintFrames
  default_planner_request_adapters/FixWorkspaceBounds
  default_planner_request_adapters/FixStartStateBounds
  default_planner_request_adapters/FixStartStateCollision
  default_planner_request_adapters/FixStartStatePathConstraints

arm:
  default_planner_config: RRTConnect
  planner_configs:
    - RRTConnect
    - RRTstar
    - BiTRRT
    - EST
    - PRMstar
  projection_evaluator: joints(joint1,joint2)
  longest_valid_segment_fraction: 0.005
  # Scaling limits each plan request; joint_limits.yaml supplies the physical
  # commissioning envelope. MIT kp/kd are separate hardware-loop parameters.
  max_velocity_scaling_factor: 0.20
  max_acceleration_scaling_factor: 0.20

  RRTConnect:
    type: geometric::RRTConnect
    range: 0.0
  RRTstar:
    type: geometric::RRTstar
    range: 0.0
    goal_bias: 0.05
    delay_collision_checking: 1
  BiTRRT:
    type: geometric::BiTRRT
    range: 0.0
  EST:
    type: geometric::EST
    range: 0.0
    goal_bias: 0.05
  PRMstar:
    type: geometric::PRMstar
